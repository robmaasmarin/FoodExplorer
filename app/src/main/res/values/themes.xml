<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FoodExplorer" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.FoodExplorer" parent="Base.Theme.FoodExplorer" />
    <style name="Theme.FoodExplorer.Splash" parent="Theme.SplashScreen">
        <!-- Color de fondo -->
        <item name="windowSplashScreenBackground">@color/mi_rojo</item>
        <!-- Icono central -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/logosquare</item>

        <!-- Color del icono -->
        <item name="windowSplashScreenIconBackgroundColor">@color/black</item>

        <!-- Tema que se aplica después -->
        <item name="postSplashScreenTheme">@style/Theme.FoodExplorer</item>
    </style>
</resources>